# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage
dist/
build/

# Local state
*.db
*.sqlite3
data/
.env
.env.local

# Editors / OS
.vscode/
.idea/
.DS_Store

# Node (web widget tooling, if added)
node_modules/

# uv writes this on `uv run`. Not committed: the project is a library plus a
# container, and the container pins its own versions at build time. Committing a
# lock generated on one contributor's machine would pin everyone else to it.
uv.lock
