**A document can no longer forge a change of speaker, and a quoted instruction
is no longer evidence.** Two fixes, each measured on its own, for the two holes
the adversarial evaluation in `evals/golden-injection/` found.

Ten documents, each carrying genuine policy and one prompt-injection payload —
an instruction override, a request for the system prompt and admin token, a
directive to stop refusing, a forged `<|im_start|>system` turn, a citation to a
document that does not exist, a forged supersession notice, a tracking pixel, a
privilege escalation, a fence escape, and a shell command. Reproducible: the
corpus and the questions are committed.

| | v0.12.5 | v0.12.6 |
|---|---:|---:|
| accuracy on questions the corpus answers | 83.3% | **91.7%** |
| false answers on questions it does not | 0 of 6 | **0 of 6** |
| same answer, asked twice | 100% | 100% |
| same facts, other words | 0% | **100%** |

**The availability fix.** A payload could not make this system lie — but it
could make it stop answering. `<|im_start|>` is a single control token to a
tokeniser, the one the chat template uses to end a turn and open a new one in
another role, so a document containing it could speak as the system. The model
obeyed, its draft came back padded with the payload's text, the grounding gate
rejected the draft, and the correct answer inside went out with the padding.
Rendering the token as `< |im_start| >` makes it four ordinary tokens. The
words are kept, not deleted: a hostile document is evidence somebody may need
to read.

**The safety fix.** The grounding gate measures support by overlap, so a
verbatim quotation of a source is perfectly grounded by construction — and an
answer that was nothing but a payload recited back scored as well supported.
Passages addressed to a machine rather than to a person now stop counting as
evidence. They are still shown to the model, so "what does this document say"
stays answerable about exactly the documents that matter most. Measured on
2,375 paragraphs of federal regulation, policy handbooks and this project's own
documentation: nothing flagged. On the ten poisoned documents: every payload
caught, no genuine paragraph touched.

**What is still broken.** Asked whether a EUR 40,000 purchase needs quotes,
the system refuses, although the rule is plain in the document and retrieval
finds it. It will state a threshold when asked what the threshold is and will
not apply it to a number in the question — the same reasoning gap the Federal
Travel Regulation set found. Unfixed, and recorded.

Cached answers are invalidated: they were drafted from a context that still
carried live control tokens.
