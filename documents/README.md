# Sample documents

These are example policies so a fresh checkout answers questions immediately. Replace them
with your own, or point `OK_DOCUMENTS_DIR` somewhere else, then run `openknowledge index`.
